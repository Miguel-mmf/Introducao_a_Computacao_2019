#include <stdio.h>

int main()
{
    int

    printf("Digite sua string: ");
    gets(str);

    for(i=0;str[i]!='\0';i++)
        if(str[i]==' '){
            esp[n]=i;
            n++;
        }

    for(i=0;str[i]!='\0';i++){
        if()
    }
}
